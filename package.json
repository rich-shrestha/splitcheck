{
  "name": "splitcheck",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "deploy": "git add -A && git commit -m 'update' && git push && npx vercel --prod"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "heic-convert": "^2.1.0",
    "jose": "^5.0.0",
    "sharp": "^0.34.5"
  }
}
